import '../../../import.dart';

class LogReferencePage extends StatelessWidget {
  const LogReferencePage();

  @override
  Widget build(BuildContext context) {
    return ReferenceTemplate(
        "Log4Shell",
        "Beginner Level",
        "References/Credits",
        "Below are our sources",
        "This is the references",
        "/log_setting_up",
        "/beginner");
  }
}
